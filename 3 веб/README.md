# 🌐 Веб-разработка

Набор из 4 лабораторных работ по веб-технологиям и фреймворкам.

## 📁 Структура

Каждая лабораторная работа находится в отдельном репозитории и добавлена как git submodule:

| Лаба | Репозиторий | Описание |
|------|-------------|---------|
| **web1** | [web1](./web1) | Базовые концепции веб-разработки |
| **web2** | [web2](./web2) | Продвинутые техники и инструменты |
| **web3** | [web3](./web3) | Фреймворки и архитектура |
| **web4** | [web4](./web4) | Комплексный проект с интеграцией всех техник |

---

## 🚀 Работа с субмодулями

### Клонирование всего репозитория с субмодулями:
```bash
git clone --recursive https://github.com/Antichnoste/MyStudy.git
```

### Обновление всех субмодулей:
```bash
git submodule update --remote
```

### Работа в отдельной лабе:
```bash
cd web1
# Здесь вы работаете в отдельном git репозитории
git status
git pull origin main
```

### Обновление субмодуля с последними изменениями из его репозитория:
```bash
cd web1
git pull origin main
cd ..
git add web1
git commit -m "Update web1 to latest version"
```

---

## 📝 Добавление README для каждой лабы

Каждая лаба уже имеет свой репозиторий и README. Для более полной документации в текущем репозитории можно создать локальные файлы:

```
3 веб/
├── README.md (этот файл)
├── web1/
│   ├── .git (отдельный репозиторий)
│   └── README.md
├── web2/
│   ├── .git (отдельный репозиторий)
│   └── README.md
├── web3/
│   ├── .git (отдельный репозиторий)
│   └── README.md
└── web4/
    ├── .git (отдельный репозиторий)
    └── README.md
```

---

## 🔄 Переписывание web4 с новой веткой

Для переписывания web4 с новой веткой:

```bash
cd web4

# Создаем новую ветку
git checkout -b develop

# Вносим изменения
# ... ваш код ...

# Коммитим изменения
git add .
git commit -m "Rewrite web4 with new branch"

# Публикуем ветку
git push origin develop

# После успешного тестирования - мерджим в main
git checkout main
git merge develop
git push origin main
```

---

## ✅ Полезные команды

| Команда | Описание |
|---------|---------|
| `git submodule status` | Показать статус всех субмодулей |
| `git submodule foreach git status` | Проверить статус в каждом субмодуле |
| `git submodule foreach git pull` | Обновить все субмодули |
| `git clone --recursive <url>` | Клонировать репо с субмодулями |

---

**GitHub**: https://github.com/Antichnoste/web1, web2, web3, web4
